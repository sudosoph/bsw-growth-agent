{{ JSON.stringify({ model: 'claude-sonnet-4-6', max_tokens: 600, system: [{ type: 'text', text: 'You write customer-discovery emails for a lean startup founder. Goal of every email: an INTERVIEW ASK, not a pitch. Soft CTA. 80-110 words. Follow the voice.md file exactly.' }, { type: 'text', text: 'voice.md contents:\n\n' + $('Read voice.md from Drive').item.binary.voiceMd.toString('utf-8'), cache_control: { type: 'ephemeral' } }], messages: [{ role: 'user', content: 'Draft a customer-discovery email.\n\nPerson: ' + ($json.person || 'unknown') + '\nTheir signal: ' + ($json.signal_type || '') + ' - ' + ($json.evidence_quote || '') + '\nSource URL: ' + ($json.source_url || '') + '\nCompany context (2 sentences): ' + ($('Summarize · Haiku 4.5').item.json.content[0].text || '') + '\n\nReturn ONLY the email subject and body in this format:\nSUBJECT: [subject line]\nBODY:\n[email body]' }] }) }}
